export class Funcionarios {
    constructor(id, salario, data_admissao, usuarioId) {
        this.id = id;
        this.salario = salario;
        this.data_admissao = data_admissao;
        this.usuarioId = usuarioId;
    }
}
